// @seawall/dashboard — Step 0 skeleton (v2 island).
//
// Pins the v2 `@mysten/sui` tree (deepbook-v3 + dapp-kit peer-require v2). The
// full Vite + React + dapp-kit app — live gauge, ModelInternals, polled on-chain
// ActionLog, the &GovernanceCap DAO-override panel, the attack panel — lands in
// Step 6. This file is intentionally trivial so Step 0 typechecks cleanly while
// the dependency island exists for the `pnpm why @mysten/sui` split proof.
console.log("[dashboard] skeleton (v2)");
export {};
