# VENDORED from deepbookv3 testnet-v19.0.0 (commit 190ab8fd), Apache-2.0.
# WHY VENDORED: the live SUI_DBUSDC pool's allowed_versions includes the deepbook
# package 0x22be4cad (what the @mysten/deepbook-v3 SDK uses) but NOT the latest
# on-chain upgrade 0x74cd5657 that Sui auto-links for a git dep. A git dep has no
# way to pin an OLDER published version, so we vendor + set published-at to the
# pool-allowed package. `get_level2_ticks_from_mid` is signature-identical across
# these versions (verified on-chain), so the v19 source compiles against 0x22be4cad.
# TYPE identity stays at the original 0xfb28c4cb (matches the live pool's type).
[package]
name = "deepbook"
edition = "2024.beta"
version = "0.0.1"
published-at = "0x22be4cade64bf2d02412c7e8d0e8beea2f78828b948118d46735315409371a3c"

[dependencies]
token = { git = "https://github.com/MystenLabs/deepbookv3.git", subdir = "packages/token", rev = "e6da254cedc1f7c1adcec59b5c6edf907d513421" }

[addresses]
deepbook = "0xfb28c4cbc6865bd1c897d26aecbe1f8792d1509a20ffec692c800660cbec6982"
