packages:
  - "packages/*"

# esbuild ships its platform binary via an optional dep; allow its postinstall
# so pnpm stops flagging it as an ignored build.
allowBuilds:
  esbuild: true

verifyDepsBeforeRun: false
